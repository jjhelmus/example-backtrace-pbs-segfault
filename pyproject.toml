[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "example"
version = "0.1"

[tool.setuptools]
ext-modules = [
  {name = "example", sources = ["example_module.c"]}
]
